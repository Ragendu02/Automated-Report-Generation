# Automated-Report-Generation
# 📊 Customer Dashboard with PDF Report

This is a simple project built with **Streamlit**.  
It displays customer data with key metrics, charts, and a filter by country.  
You can also **generate and download a PDF report** summarizing the data.

---

## 🚀 How to Run

1. **Install the required libraries:**
```bash
pip install streamlit pandas fpdf
Make sure the dataset customers-100.csv is in the same folder as the Python file.

Start the app from your terminal:

streamlit run customer_dashboard_with_pdf.py

Open your browser at:
http://localhost:8501

